// Lv. 0 문자열을 정수로 변환하기

/**
 * @param {string} n_str 숫자로만 이루어진 문자열
 * @returns {number} 문자열을 정수로 변환한 값
 */

const solution = (n_str) => {
    return +n_str;
};

// test cases
console.log(solution("10")); // 10
console.log(solution("8542")); // 8542
